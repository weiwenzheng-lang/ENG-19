/**
 * Card models and card-specific play effects.
 *
 * <p>Each card implements its own play behavior, while target and color choices
 * are supplied by the game controller, AI, or network synchronization layer.</p>
 */
package cards;
